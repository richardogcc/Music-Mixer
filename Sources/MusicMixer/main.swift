import AppKit

// main.swift is the designated SwiftPM entry point — top-level code is
// allowed here and here only.
let app = NSApplication.shared
let delegate = AppDelegate()
app.delegate = delegate
app.run()
